# 나무 자동매매 기존 코드 분석 및 수정 결과

검토 기준: 2026년 9월 9일. 기존 최신 `app.py` 59,673바이트와 초기 `app_v4.py`, NH 공식 국내주식 OpenAPI 명세, NH Python SDK 0.3.1을 확인했습니다.

**핵심 결론: 매매 조건이 엄격해서 주문이 없었던 문제만으로 볼 수 없습니다. 최신 코드에 실시간 현재가 필드를 읽지 못하는 오류가 있었습니다.** 아래의 코드 결함은 확인됐지만, 사용자가 마지막에 실행했던 배포본·로그까지 확보한 것은 아니므로 실제 운영 장애 전체를 이 원인 하나로 단정하지 않습니다.

## 확인한 문제와 변경

| 문제 | 기존 코드에서 확인한 근거 | 수정본의 동작 |
|---|---|---|
| 실시간 가격이 항상 0이 될 수 있음 | `oc` 공식 체결 데이터는 `body.price`. 기존 `on_realtime_execution`이 읽는 가격 후보에는 `price`가 없고, 가격이 0이면 바로 종료 | 공식 `price`, `volume`, `offer`, `bid`, `time`, `value_won`을 명시적으로 해석. 필드 누락은 오류로 표시 |
| 잘못된 정상 수신 표시 | 종목·가격을 검증하기 전에 `ws_connected=True`와 수신 건수 증가 | 유효하고 최신인 체결만 수신 건수에 반영. 구독 응답과 실제 체결을 구분 |
| 화면과 작업 상태의 분리 위험 | 매 실행마다 전역 `runtime`을 새로 만들면서 작업 스레드만 `st.cache_resource`로 유지 | 별도 모듈의 단일 Engine 객체에 상태·스레드를 함께 보관. 재실행 후 동일 객체 유지 검증 |
| 초단기 시간 계산 왜곡 | 최근 90초 중 마지막 30틱만 남긴 뒤 20초 전과 비교한다고 표시. 거래가 많은 종목은 30틱이 1초도 안 될 수 있음 | 1초 단위 관측값을 최대 120초 보존하고 실제 10초·20초 구간으로 비교 |
| 실시간 재연결 반복 | 체결 300건마다 구독 종료. 거래가 많을수록 재구독 빈도 증가 | 연결을 유지하고 필요한 종목의 구독만 변경 |
| 현금 한도 오해 | 잔고에서 20% 증거금 기준 `orr_pbl_amt1`을 우선 사용 | 100% 현금 기준 `orr_pbl_amt4`, 종목별 `csh_orr_pbl_qty`·`csh_orr_pbl_amt` 확인 |
| 잔고 0원인데 주문 가능금액을 생성 | `cached_cash <= 0`이면 설정된 총 투자금을 대신 사용 | 0원·필드 누락·조회 실패는 신규매수 보류. 설정값으로 잔고를 만들어내지 않음 |
| 접수와 체결 혼동 | 주문 호출 성공 후 요청 수량을 가상의 보유 목록에 추가. 체결수량·체결평균가 확인 없음 | 전송·접수·부분체결·전체체결·잔량 종료·미확인 상태 분리. 확인된 체결만 보유분에 반영 |
| 주문 중복·유실 대응 부족 | 메모리의 120초 대기 후 다시 주문 가능. 예외가 나면 대기를 해제 | 전송 전 주문 의도를 영구 기록. 응답 유실 시 자동 재전송 금지. 증권사 조회와 대조 |
| 기존 보유주식도 자동매도 가능 | 계좌에서 읽은 전체 보유종목에 같은 손절·익절 적용 | 프로그램이 체결한 수량만 자동매도 관리. 계좌 전체 보유분은 별도 표시 |
| 일일 손실 제한 미작동 | `daily_loss_limit`은 선언돼 있지만 적용 코드가 없고 `local_realized`는 항상 0 | 체결 기록 기반 추정 실현손익·보유 평가손익을 계산하고 당일 손실 한도 도달 상태를 저장 |
| 재시작 후 기록 소실 위험 | 기본 데이터 경로 `/tmp/nh_trader_live.db` | 영구 폴더/서버 볼륨에 저장. 재시작 시 신규매수 자동 재개하지 않음 |
| 전시장 후보 발굴 지연 | 전 종목을 일봉 조회로 순환 | 공개 거래량 상위 후보를 NH 현재가로 검증. 공개 목록 실패 시 전체 종목 순환이며 지연을 화면에 표시 |

초기 `app_v4.py`는 실제 주문을 보내지 않는 연결 확인 버전이었습니다. 다만 최신 `app.py`에는 주문 코드가 있으므로 초기 버전의 제한만으로 최신 버전의 문제를 설명하지 않았습니다.

## 핵심 오류 재현

공식 `oc` 예시의 현재가는 **31,700원**입니다. 기존 파일에서 실제 함수 코드를 추출해 공식 예시를 입력했고 다음 결과를 얻었습니다.

| 확인 항목 | 기존 코드 | 수정 코드 |
|---|---:|---:|
| 수신 건수 증가 | 1건 | 유효 체결 확인 후 반영 |
| 가격 갱신 함수 실행 | 0회 | 현재가 31,700원 정상 해석 |
| 결과 | 수신 표시 후 가격 0으로 신호 계산 종료 | 정상 가격으로 관측봉·진입 판단 가능 |

이 예시는 가격 해석 오류의 재현입니다. 예시 종목의 당시 거래량으로 실제 매수 조건이 충족된다는 뜻은 아닙니다.

## 완료한 검증

인증 없는 자동검증 **40개 통과**. 가상 응답을 사용했으며 실제 계좌에 주문을 보내지 않았습니다.

- 공식 실시간 필드 해석, 구독 응답 제외, 누락·잘못된 숫자·종목 불일치 감지
- 첫 관측봉에 당일 누적거래량을 통째로 넣지 않음, 역순 체결·거래량 감소 처리, 날짜 전환 초기화
- 실제 20초 관측 후 진입점수 70점 이상 생성, 1,000틱이 들어와도 1초를 20초로 오인하지 않음
- 오래된 체결·연결 공백·거래량 미달 시 진입 차단
- 잔고 0원, 동일 종목 주문 대기, 운영시간 외, 전체정지, 당일 손실 한도에서 신규매수 차단
- 주문 접수만으로 보유수량을 만들지 않음, 응답 유실 후 재전송 금지
- 부분체결 중복 조회 시 수량이 중복 누적되지 않음, 시장주문번호와 통합주문번호가 다른 가상 사례 연결
- 일치 주문이 여러 건이면 자동 연결 중단, 연결된 주문의 종목·방향·수량·가격이 달라지면 중단
- 실제 전략 신호 → 가상 주문 접수 → 체결 확인 → 익절 매도 → 보유분 종료 흐름
- 기존 수동 보유수량 자동매도 제외, 자동매매 보유분만 매도
- 재시작 후 미확인 주문 보존 및 중단 상태 유지
- Streamlit 실제 실행 테스트 도구에서 화면 생성 및 재실행 후 Engine 객체·수신 건수 유지
- 주문 요청값을 확보한 공식 명세의 필수 항목·자료형과 대조

설치된 의존성의 충돌 검사도 통과했습니다. 로컬 서버의 상태 확인 경로와 앱 HTML 모두 HTTP 200 응답을 확인했습니다. NH 공식 공개 종목마스터를 실제 SDK로 불러와 필터 적용 후 숫자 종목코드 2,436개가 해석되는 것을 확인했습니다. 내려받은 공개 거래량 목록에서는 숫자 종목코드 81개가 해석됐고, 지수 표시 선택자도 확인했습니다. 이것은 NH 인증·실시간 접속 확인과 별개입니다.

## 아직 검증하지 못한 부분

| 항목 | 현재 상태 및 의미 |
|---|---|
| 사용자 앱키의 활성 상태·계좌 권한 | 실제 인증값 없이 확인 불가. 앱의 연결 점검 단계가 필요 |
| 사용자 서버의 실시간 포트·TLS·연결 한도 | 실제 NH WebSocket 접속 미검증. 프로그램의 구독·수신·오류 표시로 확인해야 함 |
| 실계좌 주문 접수·IOC 지원·부분체결 | 공식 명세에 따라 구현했으나 실계좌 주문을 보내 확인하지 않음 |
| 실제 주문 번호 대응 | 공식 문서는 주문 응답에 시장주문번호, 주문조회에 통합주문번호를 사용. 번호가 같다고 가정하지 않고 신규 내역의 여러 조건을 대조. 동시 동일 수동주문은 항상 구분할 수 없어 계좌 단독 운영 필요 |
| 실제 수수료·세금·체결 지연 | 비용 0.3%는 추정 여유분. 실제 부과율이나 정산액을 확인한 값이 아님 |
| 전략 성과 | 역사적 시세 백테스트·실매매 성과 검증을 하지 않음. 기본값을 최적 수익 조건으로 표현하지 않음 |
| 모바일 주소·상시 운영 | 반응형 화면과 배포 파일 제공. 기존 GitHub 저장소·앱 주소·상시 서버를 확인하지 못해 배포하지 않음 |

따라서 현재 결과는 **실제 오류를 수정하고 기능 검증을 마친 재구현 소스**입니다. **실계좌 정상 운영을 확인한 완성 서비스**로 표현하지 않습니다. 실제 계좌 연결과 실행 환경을 확인한 뒤 남은 연동 차이를 점검해야 합니다.

## 기준 자료

- [NH 공식 국내주식 OpenAPI 명세](https://www.nhplug.com/openapi-docs/krstock/openapi.json): 현재가·잔고·가능수량·주문·주문조회·실시간 `oc` 필드
- [NH 공통 규약](https://www.nhplug.com/llms.txt): 응답 성공 판단, 연속조회, 통신 규약
- [NH 공식 Python SDK](https://github.com/PLUG-OpenAPI/nhplug-sdk): 인증, 요청, WebSocket 연결 규칙
- [NH 실시간 SDK 소스](https://github.com/PLUG-OpenAPI/nhplug-sdk/blob/main/nhplug/realtime.py): 구독 확인 응답과 시세 데이터 구분, 서버 한도
- [Streamlit 자원 캐시 문서](https://docs.streamlit.io/develop/api-reference/caching-and-state/st.cache_resource): 공유 자원의 수명과 스레드 안전성
- [네이버 거래량 상위](https://finance.naver.com/sise/sise_quant.naver?sosok=0): 빠른 후보 발굴용 공개 목록. 주문 결정 시세는 NH KRX 기준

원본 `app.py` SHA-256: `7372056abadd1794368d9c19d72dfe1b9268a0c0fca4a8e7beddecb494556f3b`

확인한 공식 국내주식 명세 SHA-256: `4638f64f6db11e56964d319f27dbc221f18af12f492032d7f4b8d776b8d26a8e`
