# 나무 자동매매 수정본 — 처음 실행하기

기존 최신 `app.py`를 분석해 다시 만든 Python 프로그램입니다. 화면과 매매 엔진을 분리했고, 모바일 화면을 지원합니다. 기본 설정은 총 투자한도 100만 원, 1회 주문 25만 원, 손절 0.8%, 익절 1.5%입니다. 이전 요청의 값을 유지했으며 수익성이 검증된 최적값은 아닙니다.

**현재 상태: 프로그램 구현 및 인증 없는 검증 완료 / 실제 계좌 연결·주문·체결 미검증 / 배포 미실시.**

## 휴대폰에서 사용하려면

휴대폰은 운영 화면을 여는 역할입니다. 자동매매 프로그램은 계속 켜진 PC나 서버에서 실행되어야 합니다. 이 압축파일 자체가 휴대폰에서 설치되는 APK는 아닙니다.

기존 GitHub·서버 프로젝트가 있다면 이 폴더 전체를 새 버전으로 반영하고 기존 실행 설정을 확인해야 합니다. **`app.py`만 교체하면 안 됩니다.** `nhtrader` 폴더와 `requirements.txt`도 함께 필요합니다. 기존 앱 주소와 저장소 링크가 있으면 어느 환경에 반영할지 확인할 수 있습니다.

항상 켜지는 서버, 영구 데이터 저장, HTTPS 접속이 준비되면 휴대폰 브라우저에서 접속해 홈 화면에 추가할 수 있습니다. 화면을 닫아도 서버 프로세스가 살아 있는 동안 감시는 계속됩니다. 서버가 잠자기 상태가 되거나 재시작하면 새 주문은 중단되고 다시 시작 버튼을 눌러야 합니다. 기존 Streamlit Community Cloud의 지속 운영 여부는 별도 확인이 필요합니다.

## PC에서 실행

1. 압축을 풉니다. Python 3.11 이상을 설치합니다.
2. Windows는 `start_windows.bat`를 실행합니다. 처음에 열리는 `.env`에 나무 앱키, 시크릿, 앱 비밀번호를 입력하고 저장합니다.
3. 브라우저의 로그인 화면에서 설정한 앱 비밀번호를 입력합니다.
4. **실전 계좌 불러오기 → 계좌 선택 → 선택 계좌 연결 점검**을 누릅니다.
5. 총 투자한도·1회 주문금액·손절·익절을 확인합니다.
6. 운영을 원할 때 **자동매매 시작**을 누릅니다. 이때부터 조건을 충족하면 실전 계좌에 주문을 전송합니다.

앱키와 시크릿은 `.env` 또는 배포 서비스의 비밀값 설정에 입력합니다. 채팅·스크린샷·GitHub 코드에 넣지 않습니다. 이전 이름인 `NH_APP_KEY`, `NH_APP_SECRET`도 읽을 수 있습니다.

| 설정 이름 | 입력할 내용 |
|---|---|
| `NHPLUG_APP_KEY` | 나무 OpenAPI에서 발급한 앱키 |
| `NHPLUG_APP_SECRET` | 나무 OpenAPI 시크릿 |
| `APP_PASSWORD` | 운영 화면에 로그인할 비밀번호 |
| `NH_TRADER_DATA_DIR` | 기본 `./data`, 서버에서는 영구 저장되는 위치 |

Windows 실행 화면이 닫히면 프로그램도 멈춥니다. Mac/Linux는 `.env.example`을 `.env`로 복사해 입력한 뒤 `bash start.sh`를 실행합니다. PC 실행 기본 접속 주소는 해당 PC의 `http://localhost:8501`입니다. 휴대폰 외부 접속 주소는 서버 배포 후 별도로 생깁니다.

## 버튼과 화면

| 항목 | 동작 |
|---|---|
| 자동매매 시작 | 계좌와 주문내역 재확인 후 실시간 감시·자동주문 활성화 |
| 신규매수 정지 | 신규매수만 멈추고 자동매매 보유분의 매도 관리 계속 |
| 전체정지 | 이후 주문 전송 중단. 접수된 주문의 체결조회는 계속 |
| 현황판 | 감시 종목, 실제 수신 건수, 진입점수, 대기 이유, 마지막 신호·체결 시간 |
| 보유·주문 | 계좌 잔고와 자동매매 보유수량, 접수·부분체결·완료 상태 |
| 점검 기록 | 연결·후보조회 오류 및 계좌번호를 제외한 점검 파일 다운로드 |

전체정지는 이미 접수된 주문을 되돌리거나 보유주식을 모두 매도하는 버튼이 아닙니다. 주문이 진행 중이면 해당 요청의 처리가 끝난 다음 정지가 반영될 수 있습니다. 나무 앱에서도 접수·체결 상태를 확인할 수 있습니다.

## 실제 적용하는 매매 규칙

- KRX 시세 기준 당일 거래량 **100만 주 이상**, 거래대금 **5억 원 이상**인 후보를 감시합니다. 거래량 상위 공개 목록은 후보 발굴용이고, 최종 조건과 주문 가격은 NH 데이터로 확인합니다. 전체 100만 주 이상 종목을 동시에 감시하는 구조는 아닙니다.
- 일반 국내주식 후보 중 관리·거래정지·일부 위험종목과 ETF/ETN을 제외합니다. 실시간 구독 한도에 맞춰 자동매매 보유분을 우선하고 총 10종목까지 감시합니다.
- 신규매수 시간은 평일 09:10~14:50입니다. 15:15부터 자동매매 보유분 정리를 시도하며 15:20 이후에는 새 주문을 보내지 않습니다. 거래정지·호가 공백·통신 장애가 있으면 정리가 완료되지 않을 수 있습니다.
- 감시 시작 시점부터 거래소 체결시각으로 5분봉을 관측합니다. 첫 봉과 연결이 누락된 구간은 일부 데이터입니다. 과거 분봉을 이미 확보했다고 표시하지 않습니다. 최소 20초의 연속 관측 후 진입점수를 계산합니다.
- 기본 진입기준은 70점입니다. 관측봉 상승 15점, 관측봉 +0.2% 15점, 고가권 15점, 직전 체결 상승 15점, 20초간 +0.15% 20점, 최근 10초 거래량 가속 20점입니다. 호가 간격 0.3% 초과, 데이터 3초 초과 지연, 관측봉 +2.5% 이상 과열이면 진입을 보류합니다.
- 매수는 최우선 매도호가, 매도는 최우선 매수호가의 **지정가 IOC**입니다. 해당 가격에서 가능한 수량을 체결하고 남은 수량을 취소하는 조건입니다. 가격·유동성에 따라 미체결 또는 부분체결이 발생할 수 있습니다.
- 최대 3종목, 당일 신규매수액은 총 투자한도의 75%, 최대 매수 시도 6회, 동일 종목 재진입 대기 20분입니다. 주문금액에 0.5% 여유를 두며 증권사가 반환한 미수 없는 현금 가능수량·금액도 함께 제한합니다.
- 손절·익절 외에 기본 0.6% 상승 후 고점 대비 0.6% 하락하면 이익 보호 매도를 시도합니다. 당일 추정 손실 5만 원에 도달하면 신규매수를 멈추고 보유분 정리를 시도합니다. 손익은 비용 0.3% 여유분을 적용한 추정값이며 실제 정산액과 다를 수 있습니다.

## 주문 결과 확인이 필요하다고 나올 때

주문 응답이 끊겼다는 이유로 같은 주문을 재전송하지 않습니다. 주문 직전 기록과 이후 증권사 주문내역을 비교해 종목·방향·수량·지정가격·시각이 유일하게 일치할 때만 연결합니다. 시장주문번호와 통합주문번호를 같은 번호로 가정하지 않습니다.

**동일 계좌에서 다른 자동매매 프로그램이나 같은 조건의 수동주문을 동시에 실행하지 마세요.** 현재 공식 REST 명세만으로는 동시에 들어간 동일 주문을 항상 구분할 수 없습니다. 여러 주문이 일치하거나 실제 전송 여부를 확인하지 못하면 신규매수를 보류합니다.

이 상태에서는 나무 앱의 해당 날짜·시간 주문내역과 점검 파일을 함께 확인해야 합니다. `data` 폴더를 삭제해서 잠금을 해제하면 안 됩니다. 기록이 사라지면 기존 자동매매 체결분을 식별할 수 없습니다. 증권사 기록과의 대응이 확인되지 않는 사례는 추가 연동 수정이 필요합니다.

## 서버 배포용 파일

`Dockerfile`과 `compose.yaml`을 포함했습니다. 서버에서 `.env`를 설정한 후 `docker compose up -d --build`로 실행할 수 있습니다. 데이터는 `trader-data` 볼륨에 보존됩니다. 기본 포트는 서버의 `127.0.0.1:8501`에만 열며, 휴대폰 외부 접속은 HTTPS 프록시를 통해 연결해야 합니다. 이 서버 설정·배포 자체는 이번 작업에서 실행하지 않았습니다.

자동매매는 계좌당 하나의 인스턴스에서 운영합니다. 같은 데이터 폴더의 중복 실행은 프로그램이 막습니다. 서로 다른 서버에 복제한 인스턴스 간 잠금까지 제공하지는 않습니다.

## 점검

`python check_connection.py`는 계좌·현재가·주문내역을 **조회만** 하고 `connection_check.json`을 만듭니다. 주문 전송은 하지 않습니다. 실제 WebSocket 체결 수신 상태는 앱 현황판에서 확인합니다.

개발 검증은 `pip install -r requirements-dev.txt` 이후 `python -m pytest -q`로 재실행할 수 있습니다. 포함된 테스트는 합성 체결과 가상 증권사 응답을 사용합니다.

API 지원 범위와 필드 기준: [NH 공식 국내주식 명세](https://www.nhplug.com/openapi-docs/krstock/openapi.json), [NH 공식 Python SDK](https://github.com/PLUG-OpenAPI/nhplug-sdk), [서비스 이용안내](https://www.nhplug.com/about-openapi).

